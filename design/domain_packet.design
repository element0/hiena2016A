domain packet design.

objects:

    domain packet {
	serv
	addr
	stat
	filt
	scan		// from {access path}/{cosm}/{types}

        valu

	map
	dir
    }


domain packet/{dir}/{hiena::dirent}


DOMAIN DIRECTORY
----------------

for a given domain packet, there is a directory of contents.  this is the "child table".  it can be searched like a sql table.
