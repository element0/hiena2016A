/** this provides an api for getting cascading configuration in and out.
 */

/** Load cascading configuration "types" scanners from a directory
    anywhere on the host.  This should only be used during initialization of the filesystem.
 */
void * cosm_load_types_from_dir( char * dirname ) {
    return NULL;
}
